const baseURL = "https://api.themoviedb.org/3"

const movies = "/discover/movie"
const movie = `/movie`
const genres = "/genre/movie/list"

const urls = {
    base:movies,
    movie,
    genres
}

export {
    baseURL,
    urls
}